# This is used to create a multiline command
readData=\
    input("Enter yout name - ")
print("Your name is - " + readData);

# You can use more than one command on the same line by using ; semicolon
readData=input("Enter yout name - "); print("Your name is - " + readData);